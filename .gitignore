# Python bytecode
__pycache__/
*.py[cod]

# Local virtualenvs (the daemon runs from a conda env created by setup_env.sh)
.venv/
venv/

# Session scratch / resume notes (not part of the project)
SUMMARY.md

# User-defined custom voices — runtime state, seeded empty by the daemon and
# edited via Relay's settings inspector. Not source.
voices.json
